// tailwind.config.ts

const config = {
  theme: {
    extend: {
      fontFamily: {
        instrument: ['var(--font-instrument-sans)'],
      },
    },
  },
  darkMode: 'class',
};

export default config;
